<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{ function('wp_head') }}
</head>
<body class="{{ function('get_body_class')|join(' ') }}">
{{ function('wp_body_open') }}

{% include 'partials/header.twig' %}

<main id="content">
    {% block content %}{% endblock %}
</main>

{% include 'partials/footer.twig' %}

{{ function('wp_footer') }}
</body>
</html>
